Config = {}

Config.Status = true
Config.RadarInVehicleOnly = true
Config.MinimapBorder = true

Config.DefaultColors = { -- default hud colors
    MainColor = '#DC143C', -- supports whatever css supports BUT color inputs wont update to these colors if they aren't hex
    SecondaryColor = '#DC143C',
    BackgroundColor = '#141414'
}